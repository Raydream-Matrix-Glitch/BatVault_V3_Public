
# Web framework & ASGI server
fastapi>=0.112.0
uvicorn[standard]>=0.29.0
anyio>=4.3.0
httpx>=0.27.0
python-multipart>=0.0.9

# Database / storage drivers
python-arango>=7.9.1
redis>=5.0.3
minio>=7.2.7
argon2-cffi>=23.2          # required by minioadmin → crypto.py
argon2-cffi-bindings>=21.2
cffi>=1.16
pycryptodome>=3.15.0

# Data validation & serialization
pydantic>=2.7
pydantic-settings>=2.3
jsonschema>=4.22
orjson>=3.9

# Utilities
python-dateutil>=2.9

# OpenTelemetry API and SDK are required to emit and propagate traces.
opentelemetry-api>=1.24
opentelemetry-sdk>=1.24
opentelemetry-exporter-otlp-proto-http>=1.24
opentelemetry-instrumentation-fastapi>=0.46b0

# ─── API‑Edge runtime deps ─────────────────────────────────────────────
prometheus_client>=0.19      # /metrics endpoint

# ─── Tooling (schema codegen) ──────────────────────────────────────────
# Pin for determinism; used by scripts/codegen_schemas.sh
datamodel-code-generator==0.25.6
cryptography>=42.0.0
pynacl
